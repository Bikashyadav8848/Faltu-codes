#include<stdio.h>
int sum(int n){
  int x=0;
    for(int i=0;i<=n;i++){
    x += i;
  }
  return x;
}
int main(){
    int n;
    printf("Enter a number: ");
    scanf("%d",&n);

  printf("sum of first %d Natural number = %d ",n,sum(n));
  return 0;
}
