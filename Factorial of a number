#include <stdio.h>

int fact(int n) {
  
    // BASE CONDITION
    if (n == 0)
        return 1;
  
    return n * fact(n - 1);
}

int main() {
  int n;
  printf("Enter your Factorial number: ");
  scanf("%d",&n);
    printf("Factorial of %d : %d\n",n,fact(n));
    return 0;
}
